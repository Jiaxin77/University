package condition;

import java.util.ArrayList;
import java.util.Iterator;

public class add {
	/*
	public static void main(String[] args) {
	//ArrayList<Room_state> roomList=new ArrayList<Room_state>();
	//Room_state test=new Room_state();
	Add_state test=new Add_state();
	NewTime time=new NewTime();
	Report report =new Report();
	String room_id="8528";
	String in_time=null;
	String out_time="2018-09-09 12:00";
	int mode=1;int speed=1;float current_temp=25;float target_temp=24;String start_time=null;
	String stop_time=null;
	float fee= 15.499999f;
	float fee_rate=1;
	*/
	//test.add_state(room_id, mode, speed, current_temp, target_temp, start_time, stop_time, fee,fee_rate, stop_time, speed);
	//test.add_in_out(room_id, in_time, out_time);
	//test.readBill();
	//System.out.println(roomList);
	/*ArrayList<Room_state> roomList=test.select();
	 Iterator<Room_state> it=roomList.iterator();  
     while(it.hasNext()){  
  	   Room_state p=(Room_state)it.next(); //Object类型强制转换为子类型Person，才能使用子类型的getName和getAge方法  
         System.out.println(p.getnowTime()+"--");  
        // System.out.println(it.next());  
     }  */
	//String s="314A";
	//boolean x=test.isVIP(s);
   //String t= time.SelectNewTime();
    //System.out.println(x);
	//report.setStartTime("2015-05-09");
	//System.out.println(report.getStartTime());
	//report.select("2018-05-05", "2018-06-08");
	//report.select("2015-05-05", "2018-06-07");
	
	}

